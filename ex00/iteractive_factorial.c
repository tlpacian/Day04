#include <stdio.h>

int ft_iteractive_factorial(int nb)
{
	int number;

	number = 1;
	if (nb < 0)
		return (0);
	else if (nb > 5)
		return (0);
	else
	   while (nb != 0)
           {
              fact *= nb
	      nb--;
	   }
	return (number);
}

int main(void){
  int a = 5
  int number = ft_iteractive_factorial(a);

  ft_putnbr(5);
  return 0;
