#include <stdio.h>

int ft_iteractive_power(int nb, int power)
{
	int i;
	int count;

	i = 0;
	count = 1;
	
	if (power < 0)
		return (0);
	while (i < power)
	{
	  count *nb = count;
	  return (count);
	}
}
